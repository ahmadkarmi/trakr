{
  "name": "trakr",
  "version": "1.0.0",
  "description": "Modern Audit Management - React Web & React Native Mobile",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm-run-all -p dev:web dev:mobile",
    "dev:web": "npm run dev --workspace=@trakr/web",
    "dev:mobile": "npm run dev --workspace=@trakr/mobile",
    "build:web": "npm run build --workspace=@trakr/web",
    "build:mobile": "npm run build --workspace=@trakr/mobile",
    "lint": "npm run lint --workspaces",
    "type-check": "npm run type-check --workspaces",
    "clean": "npm run clean --workspaces && rimraf node_modules",
    "install:all": "npm install",
    "seed:db": "node -r dotenv/config scripts/seed-with-credentials.js",
    "set-passwords": "node -r dotenv/config scripts/set-user-passwords.js",
    "sync-auth": "node -r dotenv/config scripts/sync-auth-ids.js",
    "postinstall": "node -e \"if (process.platform === 'linux' && process.arch === 'x64') { try { require('@rollup/rollup-linux-x64-gnu'); } catch { require('child_process').execSync('npm install --no-save --force @rollup/rollup-linux-x64-gnu', { stdio: 'inherit' }); } }\""
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "dotenv": "^17.2.3",
    "eslint": "^8.55.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.1.0",
    "rimraf": "^5.0.5",
    "@vitejs/plugin-react": "^4.2.0",
    "vite-bundle-analyzer": "^0.7.0",
    "vite": "^7.1.7",
    "typescript": "^5.3.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.30.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-org/trakr.git"
  },
  "keywords": [
    "audit",
    "management",
    "react",
    "react-native",
    "typescript",
    "monorepo"
  ],
  "author": "Your Organization",
  "license": "MIT",
  "dependencies": {
    "@tanstack/react-query-persist-client": "^5.90.2"
  }
}
