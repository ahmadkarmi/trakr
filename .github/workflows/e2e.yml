name: E2E Tests
# This workflow always triggers (no path filter on `on:`) so the `e2e`
# required status check always reports for every PR — path-filtering the
# *trigger* meant PRs outside apps/web|packages/shared|this file never got
# an e2e run at all, and branch protection then blocks merge forever
# waiting on a check that will never arrive. Instead we path-filter inside
# the job: the "Detect relevant changes" step decides whether the real
# suite needs to run, and every step after it is skipped (not failed) when
# it doesn't — so irrelevant PRs still get a fast, real "success".

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

# The suite seeds and mutates a single shared Supabase database; two runs
# executing at once corrupt each other's fixtures. Serialize repo-wide.
concurrency:
  group: e2e-shared-db
  cancel-in-progress: false

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      BASE_URL: http://localhost:3002
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Detect relevant changes
        id: changes
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "relevant=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          CHANGED=$(git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}")
          echo "$CHANGED"
          if echo "$CHANGED" | grep -qE '^(apps/web/|packages/shared/|\.github/workflows/e2e\.yml)'; then
            echo "relevant=true" >> "$GITHUB_OUTPUT"
          else
            echo "relevant=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Node
        if: steps.changes.outputs.relevant == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        if: steps.changes.outputs.relevant == 'true'
        run: npm ci

      - name: Build check
        if: steps.changes.outputs.relevant == 'true'
        working-directory: apps/web
        env:
          VITE_BACKEND: supabase
          VITE_SUPABASE_URL: ${{ secrets.E2E_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.E2E_SUPABASE_ANON_KEY }}
        run: npm run build

      - name: Install Playwright Browsers
        if: steps.changes.outputs.relevant == 'true'
        working-directory: apps/web
        run: npx playwright install --with-deps

      - name: Start Vite dev server
        if: steps.changes.outputs.relevant == 'true'
        working-directory: apps/web
        env:
          VITE_BACKEND: supabase
          VITE_SUPABASE_URL: ${{ secrets.E2E_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.E2E_SUPABASE_ANON_KEY }}
        run: |
          nohup npm run dev -- --port 3002 > server.log 2>&1 &

      - name: Wait for dev server
        if: steps.changes.outputs.relevant == 'true'
        working-directory: apps/web
        run: npx wait-on@7.0.1 http://localhost:3002

      - name: Seed test database
        if: steps.changes.outputs.relevant == 'true'
        env:
          SUPABASE_URL: ${{ secrets.E2E_SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.E2E_SUPABASE_SERVICE_KEY }}
        run: node scripts/seed-with-credentials.js

      # The vitest supabase.*.integration suites hit the live DB and skip
      # themselves when VITE_SUPABASE_* is unset - which it is on ci.yml's
      # unit-test step, so they've never run in CI. Run them here, right after
      # the seed, where the DB and secrets exist. A .env file is written
      # because import.meta.env.VITE_* is populated from Vite's env files, not
      # the process env. This runs before the Playwright suite (both mutate
      # the shared DB); the whole workflow is serialized by the concurrency
      # group, and vitest's fileParallelism:false serializes the suites.
      - name: Run integration tests (vitest, live DB)
        if: steps.changes.outputs.relevant == 'true'
        working-directory: apps/web
        env:
          E2E_SUPABASE_URL: ${{ secrets.E2E_SUPABASE_URL }}
          E2E_SUPABASE_ANON_KEY: ${{ secrets.E2E_SUPABASE_ANON_KEY }}
        run: |
          {
            echo "VITE_BACKEND=supabase"
            echo "VITE_SUPABASE_URL=$E2E_SUPABASE_URL"
            echo "VITE_SUPABASE_ANON_KEY=$E2E_SUPABASE_ANON_KEY"
            echo "VITE_TEST_EMAIL=admin@trakr.com"
            echo "VITE_TEST_PASSWORD=Password@123"
          } > .env
          npm run test:run
          rm -f .env

      - name: Debug BASE_URL and /login HTML
        if: steps.changes.outputs.relevant == 'true'
        run: |
          echo "BASE_URL=$BASE_URL"
          curl -sS -D - "$BASE_URL/login" | head -n 80

      - name: Ensure BASE_URL visible to Playwright
        if: steps.changes.outputs.relevant == 'true'
        working-directory: apps/web
        run: node -e "console.log('Playwright sees BASE_URL=', process.env.BASE_URL)"

      - name: Run E2E tests
        if: steps.changes.outputs.relevant == 'true'
        working-directory: apps/web
        env:
          E2E_SUPABASE_URL: ${{ secrets.E2E_SUPABASE_URL }}
          E2E_SUPABASE_SERVICE_KEY: ${{ secrets.E2E_SUPABASE_SERVICE_KEY }}
          E2E_SUPABASE_ANON_KEY: ${{ secrets.E2E_SUPABASE_ANON_KEY }}
        run: npm run e2e -s

      - name: Upload Playwright artifacts (always)
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-artifacts
          path: |
            apps/web/test-results
            apps/web/playwright-report
            apps/web/server.log
          if-no-files-found: ignore
